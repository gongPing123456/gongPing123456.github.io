<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://gongPing123456.github.io</id>
    <title>少年的小破站</title>
    <updated>2021-06-03T07:15:38.706Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://gongPing123456.github.io"/>
    <link rel="self" href="https://gongPing123456.github.io/atom.xml"/>
    <subtitle>🚆早一点，再早一点完善我的技术栈😊</subtitle>
    <logo>https://gongPing123456.github.io/images/avatar.png</logo>
    <icon>https://gongPing123456.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, 少年的小破站</rights>
    <entry>
        <title type="html"><![CDATA[链表反转]]></title>
        <id>https://gongPing123456.github.io/post/lian-biao-fan-zhuan/</id>
        <link href="https://gongPing123456.github.io/post/lian-biao-fan-zhuan/">
        </link>
        <updated>2021-06-03T07:08:05.000Z</updated>
        <summary type="html"><![CDATA[<p>😭算法太难了 ，基本的一个做了好久 ，还抄了答案</p>
]]></summary>
        <content type="html"><![CDATA[<p>😭算法太难了 ，基本的一个做了好久 ，还抄了答案</p>
<!-- more -->
<figure data-type="image" tabindex="1"><img src="https://gp-bucket.oss-cn-beijing.aliyuncs.com/img/title.png" alt="" loading="lazy"></figure>
<pre><code class="language-java">// 链表结构
public class ListNode {
    int val;
    ListNode next;
    ListNode(int x){val =x;}
}
</code></pre>
<h3 id="第一步-反转整个链表">第一步 ：反转整个链表</h3>
<pre><code class="language-java">/**
     * 递归反转整理链表
     * @param head
     * @return
     * 1-&gt;2-&gt;3
     * 1-&gt;2 第一步 返回一个 2
     以 1-&gt;2-&gt;null 为例子
     */
    ListNode reverse(ListNode head){
        if (head.next == null){return head;}
        ListNode listHead =reverse(head.next);// 返回2  2-&gt;null
        head.next.next =head;// 1-&gt;2 -&gt;null   那么将head（1） 给 2-&gt;1
        head.next =null;
        return listHead;
    }
</code></pre>
<h4 id="反转整个链表2">反转整个链表（2）</h4>
<pre><code class="language-java">// 反转的 另一种方法
    public  ListNode reverseList(ListNode head){
        //  1- &gt;  2  -&gt; 3
        // 前一个节点
        ListNode pre =null;
        // 当前节点 1
        ListNode cur =head;
        // 下一个节点
        ListNode next=null;

        while (cur != null){
            // 下一个节点  =  当前节点的下一个节点
            // next = 节点 2
            // next =节点 3
            // next = null
            next =cur.next;
            // 当前节点的下一个节点 =  前一个节点
            //  节点 1 .next = null
            // 节点 2.next =节点 1
            // 节点3 .next = 节点2
            cur.next=pre;
            // 前一个节点  = 当前节点
            // pre = 节点 1
            // pre = 节点 2
            // pre =节点 3
            pre =cur;
            // 当前节点 = 下一个节点
            // cur = 节点 2
            // cur =节点 3
            // cur =null
            cur=next;
        }
        return pre;
    }
</code></pre>
<h3 id="第二步反转前n个节点">第二步：反转前N个节点</h3>
<pre><code class="language-java">    /**
     * 递归反转整理链表
     * @param head
     * @return
     * 1-&gt;2-&gt;3 为例子
     * 反转前两个
     * 第一步 ：   将2 下一个取出来  nextNode =3
     *  将 1 下一个 节点 2  的下一个节点  3  变为 1
     *  2 和 1 节点反转
     *  随后将 1 的下一个节点 变为nextNode
     *  2-&gt;1-&gt;3
     *  在返回 第二个节点 也就是 2
     */
    ListNode nextNode =null;
    ListNode reverse(ListNode head,int n){
//      if (head.next == null){
//
//          return head;
//      }
        if (n == 1){
            nextNode=head.next;
            return head;
        }
        ListNode listHead =reverse(head.next,n-1);// 返回2  2-&gt;null
        head.next.next =head;// 1-&gt;2 -&gt;null   那么将head（1） 给 2-&gt;1
        //head.next =null;
        head.next =nextNode;
        return listHead;
    }
</code></pre>
<h3 id="第三步-反转n-m">第三步： 反转n-m</h3>
<pre><code class="language-java">public ListNode betweenListNode(ListNode head,int m ,int n){
        // 如果为1 以后 就是反转  链表的  前 n个节点
        if (m == 1){
            return reverse(head,n);
        }
        // 采用 m 减少到 1  以后  n的长度正好是 n-m+1 的
        // 也就是需要 反转的数量 和位置

        // head.next 目的是为了指向 反转的节点  （head.next 传入参数 ：将下一个需要验证是否反转的节点传入）
        head.next = betweenListNode(head.next, m - 1, n - 1);
        return head;
    }
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[暂时不知道]]></title>
        <id>https://gongPing123456.github.io/post/aaaa/</id>
        <link href="https://gongPing123456.github.io/post/aaaa/">
        </link>
        <updated>2021-05-30T12:58:28.000Z</updated>
        <summary type="html"><![CDATA[<p>❓说什么耶✌</p>
]]></summary>
        <content type="html"><![CDATA[<p>❓说什么耶✌</p>
<!-- more -->
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Spring 事务]]></title>
        <id>https://gongPing123456.github.io/post/spring-shi-wu/</id>
        <link href="https://gongPing123456.github.io/post/spring-shi-wu/">
        </link>
        <updated>2021-05-30T08:45:39.000Z</updated>
        <summary type="html"><![CDATA[<p>✍️  Spring 事务基础的描述... ...</p>
]]></summary>
        <content type="html"><![CDATA[<p>✍️  Spring 事务基础的描述... ...</p>
<!-- more -->
<h3 id="事务特性-acid">事务特性 ACID</h3>
<ul>
<li>原子性 （atomicity） 要么完成要不不完成</li>
<li>一致性   (consistency)  事务开始和结束，数据库的完整性没有发生改变</li>
<li>隔离性   (Isolation)  两个事务是相互隔离开的，一个事务执行期间 ，不会被其他事务所影响</li>
<li>持久性  (Durability)  事务一旦提交，事务所处理的信息永久保存在数据库中</li>
</ul>
<h3 id="事务的隔离级别级别提升的排列">事务的隔离级别（级别提升的排列）</h3>
<ol>
<li><strong>DEFAULT （默认）</strong></li>
</ol>
<blockquote>
<p>使用数据库的默认隔离级别  Mysql 使用REPEATABLE_READ隔离级别，Oracle采用READ_COMMITTED隔离级别</p>
</blockquote>
<ol>
<li><strong>READ_UNCOMMITTED （读未提交）</strong></li>
</ol>
<blockquote>
<p>数据库允许其他事务看到该事务未提交的数据，这样的级别会造成脏读、幻读、不可重复读</p>
</blockquote>
<ol>
<li><strong>READ_COMMITTED （读已提交）</strong></li>
</ol>
<blockquote>
<p>一个事务修改提交后的数据才允许被其他事务读取，另个事务不能读取该事务未提交的数据，这种级别可以避免出现脏读，但是会造成不可重复读和幻读</p>
</blockquote>
<ol>
<li><strong>REPEATABLE_READ （可重复读）</strong></li>
</ol>
<blockquote>
<p>这种事务隔离级别可以防止脏读、不可重复读，但是可能出现幻像读。它除了保证一个事务不能读取另一个事务未提交的数据外，还保证了不可重复读。</p>
</blockquote>
<ol>
<li><strong>SERIALIZABLE（串行化）</strong></li>
</ol>
<blockquote>
<p>事务被处理为顺序执行，避免了脏读、幻读、可重复读。</p>
</blockquote>
<h3 id="脏读-幻读-不可重复读">脏读、幻读、不可重复读</h3>
<ol>
<li>脏读</li>
</ol>
<blockquote>
<p>一个事务对一组数据进行了更新，另一个事务来读取到了第一个事务未提交的更新，如果这个时候 第一个事务发生了回滚，那么第二个事务读取到的数据就是错误数据，这就是脏读</p>
</blockquote>
<ol>
<li>不可重复读</li>
</ol>
<blockquote>
<p>一个事务多次读取了一条数据，在这个期间另一个事务对该条数据进行了修改，</p>
<p>那么第一个事务几次读取的数据是不一致的，那么就造成了不可重复读。</p>
</blockquote>
<ol>
<li>幻读</li>
</ol>
<blockquote>
<p>一个事务多次读取了一部分符合条件的数据，同时另一个事务对该数据进行了新增，那么第一个事务第二次读取的时候会多出，这就是幻读</p>
</blockquote>
<p>幻读和不可重复读区别</p>
<blockquote>
<p>不可重复读 是在第二个事务进行了修改删除而产生的</p>
<p>幻读是第二个事务 进行新增操作产生的</p>
</blockquote>
<figure data-type="image" tabindex="1"><img src="https://cdn.nlark.com/yuque/0/2021/png/2650954/1622012630229-c672bab9-fc7a-4559-8cb5-e216acd41fc4.png" alt="image.png" loading="lazy"></figure>
<h3 id="spring-事务的传播行为">spring 事务的传播行为</h3>
<ol>
<li>propagation.<strong>REQUIRED</strong></li>
</ol>
<blockquote>
<p>如果当前没有事务，就创建一个事务；如果当前存在事务，则假如该事务；</p>
<p>举例有两个方法：</p>
<pre><code>1. @Transactional(propagation = Propagation.REQUIRED)
2. public void methodA() {
3.  methodB();
4. // do something
5. }
6. 
7. @Transactional(propagation = Propagation.REQUIRED)
8. public void methodB() {
9. // do something
10. }
</code></pre>
<p><em><strong>单独调用methodB方法时，因为当前上下文不存在事务，所以会开启一个新的事务。</strong></em></p>
<p><em><strong>调用methodA方法时，因为当前上下文不存在事务，所以会开启一个新的事务。当执行到methodB时，methodB发现当前上下文有事务，因此就加入到当前事务中来。</strong></em></p>
</blockquote>
<ol>
<li>propagation.<strong>SUPPORTS</strong></li>
</ol>
<blockquote>
<p><strong>支持当前事务，如果当前不存在事务，就按照非事务方式执行；</strong></p>
<pre><code>1. @Transactional(propagation = Propagation.REQUIRED)
2. public void methodA() {
3.  methodB();
4. // do something
5. }
6. 
7. // 事务属性为SUPPORTS  单独调用是非事务执行
8. @Transactional(propagation = Propagation.SUPPORTS)
9. public void methodB() {
10. // do something
11. }
</code></pre>
<p><em><strong>单纯的调用methodB时，methodB方法是非事务的执行的。当调用methdA时,methodB则加入了methodA的事务中,事务地执行</strong></em>。</p>
</blockquote>
<ol>
<li>propagation.<strong>MANDATORY</strong></li>
</ol>
<blockquote>
<p>使用当前事务，如果没有事务则抛出异常</p>
<pre><code>1. @Transactional(propagation = Propagation.REQUIRED)
2. public void methodA() {
3.  methodB();
4. // do something
5. }
6. 
7. // 事务属性为MANDATORY   单独调用会抛出异常
8. @Transactional(propagation = Propagation.MANDATORY)
9. public void methodB() {
10. // do something
11. }
</code></pre>
<p>当单独调用methodB时，因为当前没有一个活动的事务，则会抛出异常throw new IllegalTransactionStateException(“Transaction propagation ‘mandatory’ but no existing transaction found”);当调用methodA时，methodB则加入到methodA的事务中，事务地执行。</p>
</blockquote>
<ol>
<li>propagation.<strong>REQUIRES_NEW</strong></li>
</ol>
<blockquote>
<p>新建事务，如果当前存在事务，将当前事务挂起；</p>
<h4 id="外层事务不会影响内部事务的提交回滚">外层事务不会影响内部事务的提交/回滚</h4>
<h4 id="内部事务的异常会影响外部事务的回滚">内部事务的异常，会影响外部事务的回滚</h4>
<pre><code>1. @Transactional(propagation = Propagation.REQUIRED)
2. public void methodA() {
3. doSomeThingA();
4. methodB();
5. doSomeThingB();
6. // do something else
7. }
8. 
9. 
10. // 事务属性为REQUIRES_NEW
11. @Transactional(propagation = Propagation.REQUIRES_NEW)
12. public void methodB() {
13. // do something
14. }
</code></pre>
<p>当调用</p>
<pre><code>1. main{  
2. methodA();
3. }
</code></pre>
<p>相当于调用</p>
</blockquote>
<blockquote>
<pre><code class="language-java">1. main(){
2.     TransactionManager tm = null;
3. try{
4. //获得一个JTA事务管理器
5.         tm = getTransactionManager();
6.         tm.begin();//开启一个新的事务
7.         Transaction ts1 = tm.getTransaction();
8.         doSomeThing();
9.         tm.suspend();//挂起当前事务
10. try{
11.             tm.begin();//重新开启第二个事务
12.             Transaction ts2 = tm.getTransaction();
13.             methodB();
14.             ts2.commit();//提交第二个事务
15.         } Catch(RunTimeException ex) {
16.             ts2.rollback();//回滚第二个事务
17.         } finally {
18. //释放资源
19.         }
20. //methodB执行完后，恢复第一个事务
21.         tm.resume(ts1);
22.         doSomeThingB();
23.         ts1.commit();//提交第一个事务
24.     } catch(RunTimeException ex) {
25.         ts1.rollback();//回滚第一个事务
26.     } finally {
27. //释放资源
28.     }
29. }
</code></pre>
</blockquote>
<blockquote>
<p>在这里，我把ts1称为外层事务，ts2称为内层事务。从上面的代码可以看出，ts2与ts1是两个独立的事务，互不相干。Ts2是否成功并不依赖于 ts1。如果methodA方法在调用methodB方法后的doSomeThingB方法失败了，而methodB方法所做的结果依然被提交。而除了 methodB之外的其它代码导致的结果却被回滚了</p>
</blockquote>
<ol>
<li>propagation.<strong>NEVER</strong></li>
</ol>
<blockquote>
<p>非事务方式运行，如果存在事务，则抛出异常</p>
</blockquote>
<ol>
<li>Propagation.<strong>NESTED</strong></li>
</ol>
<blockquote>
<p>支持当前事务，新增Savepoint点，与当前事务同步提交或回滚。</p>
<p>嵌套事务一个非常重要的概念就是内层事务依赖于外层事务。外层事务失败时，会回滚内层事务所做的动作。而内层事务操作失败并不会引起外层事务的回滚。</p>
</blockquote>
<blockquote>
<pre><code class="language-java">1. @Transactional(propagation = Propagation.REQUIRED)
2. methodA(){
3. doSomeThingA();
4. methodB();
5. doSomeThingB();
6. }
7. 
8. @Transactional(propagation = Propagation.NEWSTED)
9. methodB(){
10.   ……
11. }
</code></pre>
</blockquote>
<blockquote>
<p>如果单独调用methodB方法，则按REQUIRED属性执行。如果调用methodA方法，相当于下面的效果：</p>
</blockquote>
<blockquote>
<pre><code class="language-java">1. main(){
2.     Connection con = null;
3.     Savepoint savepoint = null;
4. try{
5.         con = getConnection();
6.         con.setAutoCommit(false);
7.         doSomeThingA();
8.         savepoint = con2.setSavepoint();
9. try{
10.             methodB();
11.         } catch(RuntimeException ex) {
12.             con.rollback(savepoint);
13.         } finally {
14. //释放资源
15.         }
16.         doSomeThingB();
17.         con.commit();
18.     } catch(RuntimeException ex) {
19.         con.rollback();
20.     } finally {
21. //释放资源
22.     }
23. }
</code></pre>
</blockquote>
<blockquote>
<p>当methodB方法调用之前，调用setSavepoint方法，保存当前的状态到savepoint。如果methodB方法调用失败，则恢复到之前保存的状态。但是需要注意的是，这时的事务并没有进行提交，如果后续的代码(doSomeThingB()方法)调用失败，则回滚包括methodB方法的所有操作。嵌套事务一个非常重要的概念就是内层事务依赖于外层事务。<em><strong>外层事务失败时，会回滚内层事务所做的动作。而内层事务操作失败并不会引起外层事务的回滚</strong></em>。</p>
</blockquote>
<ol>
<li>Propagation.<strong>NOT_SUPPORTED</strong></li>
</ol>
<blockquote>
<p>如果当前存在事务，则挂起事务，以非事务方式执行</p>
</blockquote>
<h3 id="spring-事务不生效原因">spring 事务不生效原因</h3>
<ul>
<li>数据库不支持事务 Mysql 中MyISAM 不支持事务，InnoDB 才支持事务</li>
<li>方法不是public</li>
</ul>
<figure data-type="image" tabindex="2"><img src="https://cdn.nlark.com/yuque/0/2021/png/2650954/1622017952228-f044832a-2d93-46d8-abe6-5de90a1165f4.png" alt="image" loading="lazy"></figure>
<ul>
<li>异常类型不对应，默认的是RuntimeException ，才会生效，触发回滚</li>
</ul>
<blockquote>
<pre><code>@Transactional(rollbackFor = Exception.class)
</code></pre>
</blockquote>
<blockquote>
<p>这个配置仅限于 <code>Throwable</code> 异常类及其子类。</p>
</blockquote>
<ul>
<li>同一个类中未配置 @Transactional</li>
</ul>
<pre><code class="language-java">    //    @Transactional    public void save() {        save2();    }    @Transactional    public void save2() {        insert(XXX)        int i = 1 / 0;    }
</code></pre>
<h3 id="spring-事务的配置方式">spring 事务的配置方式</h3>
<h4 id="编程式事务">编程式事务</h4>
<ol>
<li>使用 <code>TransactionTemplate</code> 实现事务</li>
</ol>
<pre><code class="language-java">@Autowired
    private  TransactionTemplate transactionTemplate;
    @Override
    public void selectAdd (){

        transactionTemplate.execute(new TransactionCallback&lt;Boolean&gt;() {

            @Override
            public Boolean doInTransaction(TransactionStatus transactionStatus) {
                try {
                    User user = new User();
                    user.setNum(100);
                    user.setName(&quot;.pim&quot;);
                    user.setId(100);
                    save(user);
                    int a = 1 / 0;
                    removeById(user);
                }catch (Exception e){
                    transactionStatus.setRollbackOnly();
                    log.error(e.getMessage(), e);
                    return  false;
                }
                return true;
            }
        });

    }
</code></pre>
<p>2.<code>PlatformTransactionManager</code> 实现事务</p>
<pre><code class="language-java">@Autowired
    PlatformTransactionManager transactionManager;
    @Override
    public void selectAdd (){

        DefaultTransactionDefinition def = new DefaultTransactionDefinition();
        def.setReadOnly(false);
//隔离级别,-1表示使用数据库默认级别
        def.setIsolationLevel(TransactionDefinition.ISOLATION_READ_COMMITTED);
        def.setPropagationBehavior(TransactionDefinition.PROPAGATION_REQUIRED);
        TransactionStatus status = transactionManager.getTransaction(def);
        try {
            User user = new User();
                    user.setNum(100);
                    user.setName(&quot;.pim&quot;);
                    user.setId(100);
                    save(user);
                    //int a = 1 / 0;
                    removeById(1);
            transactionManager.commit(status);
        } catch (Exception e) {
            transactionManager.rollback(status);
            log.error(&quot;异常&quot;);
        }
    }
</code></pre>
<h4 id="注解实现事务">注解实现事务</h4>
<blockquote>
<p>使用@Transactional 可以实现事务的支持</p>
</blockquote>
<pre><code class="language-java">@Override
    @Transactional
    public void selectAdd (){
            User user = new User();
                    user.setNum(100);
                    user.setName(&quot;.pim&quot;);
                    user.setId(100);
                    save(user);
                    int a = 1 / 0;
                    removeById(1);

    }
</code></pre>
<h5 id="注意-注解的方法如果不是public非public-方法无法获取事务的配置信息">注意 注解的方法如果不是<code>public</code>，非<code>public</code> 方法无法获取事务的配置信息</h5>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Hello Gridea]]></title>
        <id>https://gongPing123456.github.io/post/hello-gridea/</id>
        <link href="https://gongPing123456.github.io/post/hello-gridea/">
        </link>
        <updated>2018-12-11T16:00:00.000Z</updated>
        <summary type="html"><![CDATA[<p>👏  欢迎使用 <strong>Gridea</strong> ！<br>
✍️  <strong>Gridea</strong> 一个静态博客写作客户端。你可以用它来记录你的生活、心情、知识、笔记、创意... ...</p>
]]></summary>
        <content type="html"><![CDATA[<p>👏  欢迎使用 <strong>Gridea</strong> ！<br>
✍️  <strong>Gridea</strong> 一个静态博客写作客户端。你可以用它来记录你的生活、心情、知识、笔记、创意... ...</p>
<!-- more -->
<p><a href="https://github.com/getgridea/gridea">Github</a><br>
<a href="https://gridea.dev/">Gridea 主页</a><br>
<a href="http://fehey.com/">示例网站</a></p>
<h2 id="特性">特性👇</h2>
<p>📝  你可以使用最酷的 <strong>Markdown</strong> 语法，进行快速创作</p>
<p>🌉  你可以给文章配上精美的封面图和在文章任意位置插入图片</p>
<p>🏷️  你可以对文章进行标签分组</p>
<p>📋  你可以自定义菜单，甚至可以创建外部链接菜单</p>
<p>💻  你可以在 <strong>Windows</strong>，<strong>MacOS</strong> 或 <strong>Linux</strong> 设备上使用此客户端</p>
<p>🌎  你可以使用 <strong>𝖦𝗂𝗍𝗁𝗎𝖻 𝖯𝖺𝗀𝖾𝗌</strong> 或 <strong>Coding Pages</strong> 向世界展示，未来将支持更多平台</p>
<p>💬  你可以进行简单的配置，接入 <a href="https://github.com/gitalk/gitalk">Gitalk</a> 或 <a href="https://github.com/SukkaW/DisqusJS">DisqusJS</a> 评论系统</p>
<p>🇬🇧  你可以使用<strong>中文简体</strong>或<strong>英语</strong></p>
<p>🌁  你可以任意使用应用内默认主题或任意第三方主题，强大的主题自定义能力</p>
<p>🖥  你可以自定义源文件夹，利用 OneDrive、百度网盘、iCloud、Dropbox 等进行多设备同步</p>
<p>🌱 当然 <strong>Gridea</strong> 还很年轻，有很多不足，但请相信，它会不停向前 🏃</p>
<p>未来，它一定会成为你离不开的伙伴</p>
<p>尽情发挥你的才华吧！</p>
<p>😘 Enjoy~</p>
]]></content>
    </entry>
</feed>